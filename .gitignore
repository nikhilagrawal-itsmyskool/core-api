# package directories
node_modules

# Serverless directories
.serverless

# Webpack directories
.webpack
.env
.vscode/launch.json

lib
!shared/*
.local/

# Local Claude Code settings (per-developer, not shared)
.claude/settings.local.json

# Tooling / session artifacts
.playwright-mcp/
*.stackdump

# CP-SAT per-run artifact folders (solver-input/solution/etc.); keep the root assets
# (*.py, *.ts, HANDOFF.md, jest.config.js) tracked.
modules/timetable/cpsat/*/
modules/fees/reports/
